<div class="modal fade" id="modalAddSeries" tabindex="-1" role="dialog" aria-labelledby="coucou" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title" id="exampleModalLongTitle">ajout un livre/series</h3>
            </div>
            <div class="modal-body">
                {{ form_start(serie_form) }}
                {{ form_widget(serie_form) }}
                <button class="">{{ button_label|default('ajout') }}</button>
                {{ form_end(serie_form) }}
            </div>
            <div class="modal-footer">
                empty
            </div>
        </div>

    </div>
</div>